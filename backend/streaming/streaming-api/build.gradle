plugins {
    id 'java'
}

dependencies {
    compile project(':backend:streaming:streaming-domain')
    compile project(':backend:party:party-information:current-party-track:current-party-track-domain')
}