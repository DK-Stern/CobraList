plugins {
    id 'java'
}

dependencies {
    compile project(':backend:party:party-information:current-party-track:current-party-track-api')
    compile project(':backend:party:party-information:current-party-track:current-party-track-dataaccess-port')
    runtimeOnly project(':backend:party:party-information:current-party-track:current-party-track-dataaccess-adapter')
    compile project(":backend:streaming:streaming-api")
    runtimeOnly project(":backend:streaming:streaming-spotify-adapter")

    compile('org.springframework:spring-context:' + springVersion)
}